import React from 'react';

export default () => (
  <div id = "template-message">
    <p>
      Welcome to your template. Remember to change the database name in <code> server/db/mongoose </code>, change the model name in <code>post.js</code>review <code>package.json</code>, and run <code>git init</code>.
    </p>
    <p>
      <code>npm init</code> is not necessary.
    </p>
  </div>
);
